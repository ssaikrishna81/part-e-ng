# Development Dockerfile: runs the Angular dev server so you can mount source and hot-reload
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
# Install dependencies without optional native binaries (more reliable inside containers)
RUN npm ci --silent --no-optional
COPY . .
EXPOSE 5633
CMD ["npm", "run", "start"]
