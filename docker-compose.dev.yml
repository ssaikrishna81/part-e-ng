# Deprecated: use the shared dev stack at the repository root.
# The new setup lives in ../docker-compose.dev.yml and proxies all services
# through nginx with consistent /opal, /vaccine, /water routes.
