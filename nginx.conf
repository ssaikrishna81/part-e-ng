server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # serve static files directly
  location / {
    try_files $uri $uri/ /index.html;
  }

  # optional: increase max body size if you accept file uploads
  client_max_body_size 10M;
}
